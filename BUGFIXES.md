# Bugs

## Fixed
- Cross-page "zoom" feel — home page rendered smaller, booking/confirmation pages suddenly bigger, persisting until refresh.
- Footer "CamKit" text shown fully instead of half.
- UI not displaying correctly on big screens (≥1440px / ≥1920px).
- About page on big screens — empty space on the right of the photo marquee strip.
- About page sticky scroll not working on the "two pillars" section.
- Privacy Policy and Terms of Service pages updated to match the design (eyebrow label as title, wider card, no big h1).
- Confirmation page "what happens next" — doc icon was solid pink while others were grayed; matched it to the dark-gray 40% opacity style.
- Gear page (CAMKIT RENTALS) — clicking "details" on a product card now opens the same product details popup as the home page.
- Events portfolio redesigned — replaced static thumbnails with shoot covers (Bridal Portraits, Studio Fashion, Portrait Sessions). Clicking a cover opens a full-screen lightbox with prev/next arrows, keyboard navigation (←/→/Esc), and touch swipe support to slide through all photos in that shoot.

## Open

### Minor — Home / About
- Increase logo size and remove "Rentals & Events" text (already part of the logo image).
- Hero title accent: change "lenses" → "kit rentals".
- Reduce line height on the "photography" section paragraph.
- Arrow indicator should point at the rentals/events tab options.
- Reduce line height on testimonial card paragraphs.
- Make the floating WhatsApp button a square (no curves anywhere else in the design).
- Portfolio strip images should be top-aligned.
- Stats block: numbers left-aligned, labels right-aligned (currently both left).
- About-page photographer PNG should stick out / overflow above the green surface as in the design.
- Footer "CamKit" needs progressive blur effect per design (currently a hard fade).

### Major — Gear / Booking / Confirmation / Events
- Filter option on gear page not working properly.
- Product names and images don't match.
- Booking fulfilment options have two different active/inactive styles — make them match the payment-mode option style.
- Confirmation page heading: change "request sent" → "send request" (request hasn't actually been sent).
- Confirmation page: include Booking ID inside the WhatsApp message text.
- Confirmation page button: change "home" → "send request".
- Events page duration slider: cap at 2 days max.
- Events page: clicking "send consultation request via WhatsApp" should open a popup asking for user's name & phone first.
